#include <stdio.h>
#include <stdlib.h>
#include <armstate.h>

int main (int argv, char** argc) {
// read the binary file
	FILE *fp = fopen("input.txt", "r");
	FILE *fprocessed = fopen("processed.txt");
// populate memory with the commands
	return 0;
}
